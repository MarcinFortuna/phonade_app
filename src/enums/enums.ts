export enum Sets {
    WORDS = 'Words',
    SENTENCES = 'Sentences',
    INFLECTION = 'Inflection'
};

export enum Order {
    SEQ = "Sequential",
    RANDOM = "Random"
};

export enum Scope {
    ALL = "All",
    TEST = "20 samples"
};